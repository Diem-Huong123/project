server:
  port: 8083
  servlet:
    context-path: /khht

spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/khht_service"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
