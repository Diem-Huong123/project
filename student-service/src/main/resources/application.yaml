server:
  port: 8080
  servlet:
    context-path: /student

spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/students"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true